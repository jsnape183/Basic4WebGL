import { describe, test } from 'vitest';

// Retired scratch probe (scanned the finale map for sharp local light drops).
// Its investigation is closed; the Rc* library moved into the softRaycaster
// package so its old demo-dir file reads no longer resolve. Kept inert.
describe.skip('finale light-scan probe (retired)', () => {
  test('noop', () => {});
});
